<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme for the (single) Compose Activity. Visuals are driven by Compose;
         this only needs a sane window background and no action bar. -->
    <style name="Theme.BAAD" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
