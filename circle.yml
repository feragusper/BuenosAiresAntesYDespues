dependencies:
  pre:
    - echo 'y' | android update sdk --filter build-tools-23.0.1 --no-ui --all

deployment:
  debug: # just a label; label names are completely up to you
    branch: master
    commands:
      - ./gradlew testfairyDebug
machine:
  java:
    version:
      oraclejdk8